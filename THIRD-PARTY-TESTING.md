# 测别人的 Skill：流程与规矩

> 四层判定（CLI 发现 / 文件落盘 / 自动触发 / 任务完成）和前瞻登记的做法，
> 见[兼容性四层测试法](https://sanhuang520-ship-it.github.io/awesome-chinese-ai-tools/method/)。
> **本页只写「测别人」比「测自己」多出来的那部分。**

## 为什么做这件事

生态里已有数千个 Skill，但没有地方能查到**哪个真的会被触发、能不能完成任务**。

2026-09-03 检索：`agent skills compatibility test evidence` 仅 1 个不相关结果；
`claude skills benchmark trigger test` **0 个结果**；能找到的 `coder_eval`、
`tripwire`、`skillforge` 都是**给作者测自己 Skill 的框架**，
没有独立测第三方并公布结果的。

这是本仓库唯一有现成机具、且没人在做的事。

## 五条规矩（写在任何测试之前）

### 1. 只用它自己声明的契约当标准

这是最容易违反、后果也最严重的一条。

本仓库的 Skill 设计为**自动触发**，并逐个实测触发率。
但 `superpowers-zh` 的中文 Skill 在 `description` 里明确写着
「仅在用户显式 `/xxx` 时调用，**不要根据上下文自动触发**」。

两种都不是错，是不同的设计选择。**拿我们的标准去判它，错的是我们。**

具体做法：测试前先把目标 Skill 的 `description` 和文档里
**它自己承诺 / 拒绝的行为**摘出来，判定门槛只能从这些里面来。

> 由此产生的一个反直觉判定：对声明「不要自动触发」的 Skill，
> **「没触发」才是通过**。

### 2. 自测偏差照样存在，一个字不能少

执行任务和判定结果的是同一个模型。这条在测自己时写过，
测别人时**不许因为"测的是别人所以更客观"就省略**。

如果执行者已经知道被测 Skill 的存在或内容，**盲测已被污染**，
必须换一个未接触过测试记录的会话，并在记录里写明。

### 3. 不打分、不排名、不写好坏

只记录可观察事实：触发了没有、覆盖了哪几项、输出里有没有它自己承诺的东西。

**不写**：内容质量高低、"比 X 好用"、任何形式的评分或星级。
一旦开始打分，这份记录就从证据变成了评论。

### 4. 装之前先只读审查

和我们要求别人对待我们的 Skill 一样：先看 `SKILL.md` 和目录里有没有脚本。
可以用[本站的只读审计器](https://sanhuang520-ship-it.github.io/awesome-chinese-ai-tools/audit-skill/)，
但它只提供人工复核线索，不是安全认证。

记录里要写明：协议、目录内有无可执行文件、是否与上游逐字节一致。

### 5. 先登记后执行，给作者申辩渠道

任务原文与判定门槛**在运行前公开**，事后不改题。
结果发布后，作者可以要求复测或指出方法问题；
复测结果追加，**不覆盖首次记录**。

## 选谁来测

按这三条挑，不按"谁热"挑：

| 条件 | 为什么 |
|---|---|
| **真有人在装** | 星数或安装量能查到。没人用的东西测了没有信息量 |
| **契约写得清楚** | `description` 里有明确的触发/不触发声明，才有东西可判 |
| **测出来对别人有用** | 结果能改变某个人的安装决定 |

**不测**：本仓库自己的竞品对照、明显无人问津的项目、
以及任何我们无法在不违反第 1 条的前提下判定的目标。

## 前瞻登记模板（复制即用）

```markdown
## 前瞻登记：<skill-name>（尚未执行）

**目标**：<owner/repo> 的 `<path>` · ★<stars> · <license> · 最后提交 <date>

**它自己声明的契约**（从 description 与文档摘出，判定只能依据这些）：
- <例：仅在显式斜杠命令时调用，不要自动触发>
- <例：覆盖 A / B / C 三项>

**执行者是否已被污染**：<是/否，说明理由>

### 任务 A：<测的是它的哪一条契约>
​```text
<逐字任务原文>
​```
- 通过 = <条件>
- 失败 = <条件>

### 不会记录的
- <明确列出本轮不判定的东西>
```

## 已有记录

- [`chinese-git-workflow`（superpowers-zh ★7960）— 首例](cases/third-party-chinese-git-workflow-2026-09-03.md)
  安装层已测；触发测试已前瞻登记、尚未执行。

## 顺带产出的东西

测别人的过程会撞到工具本身的问题。目前撞到两次，方向相反：

| 场景 | CLI 报告 | 实际 |
|---|---|---|
| `update -g`，无锁条目（2026-08-28） | ✓ Updated 22 skill(s)，退出码 0 | **一个都没更新** |
| `add --skill … -g`（2026-09-03） | ✗ Failed to install 1 | **完整装好且与上游一致** |

**判断 skills CLI 是否生效，必须核对落盘文件，不能采信它的输出。**
