# `chinese-web-themes` 前瞻复测：普通博客水墨主题接入

> 执行日期：**2026-08-13** · 客户端：Codex CLI `0.147.0-alpha.6.5` · 模型：`gpt-5.6-sol` · 结果：**3 / 4，未通过全部预注册门槛**

任务与四项成功门槛先发布在 `data/retest-queue.json`，随后才运行。本记录保留失败，不根据输出降低门槛。

## 预注册任务原文

```text
我有一个普通中文博客，现有正文和代码块样式都要保留，想增加克制的水墨气质。请只推荐一套现有主题，给出最短接入步骤和上线前检查清单；不要生成完整 CSS，也不要修改文件。
```

## 隔离运行方式

- 新建临时 Git 项目，只放入 `.agents/skills/chinese-web-themes/`。
- 临时副本与当前仓库 `SKILL.md` 的 SHA-256 均为 `8aef2359e5eb21914778bc29c01f7f0964f3273dcabaa1c96e2a1b206e07ffe0`。
- 使用 `--ephemeral --ignore-user-config --sandbox read-only`；Skill 哈希未变，临时 Git 状态仅出现测试命令 `-o` 保存的响应文件。

## 输出摘要与失败点

任务未点名 Skill；Codex 主动读取 `chinese-web-themes`。它只推荐水墨 `ink`，给出四步接入，并检查正文、移动端、代码块、加载顺序/样式覆盖和对比度/键盘焦点；没有生成完整 CSS，也没有修改项目。

但上线前清单**没有检查授权**。Skill 明确说明付费字体授权需自行核实，而预注册门槛也把“授权”列为必查项，因此本次只能记为 3 / 4。

## 预注册门槛核对

| 门槛 | 结果 | 观察 |
|---|---:|---|
| 未点名 Skill 时主动读取 chinese-web-themes | ✅ | 主动声明使用中式网页主题库并读取隔离副本 |
| 只推荐一套现有主题并给出最短接入步骤 | ✅ | 只推荐 `ink`，给出四步接入 |
| 检查正文行宽、移动端、代码块、样式覆盖、授权与无障碍 | ❌ | 覆盖正文、移动端、代码块、样式覆盖和无障碍，但遗漏授权 |
| 不生成完整 CSS，不修改文件 | ✅ | 没有输出完整 CSS；只读沙箱；Skill 哈希未变 |

## 证据边界

- 这是仓库维护者自测，不是外部用户反馈。
- 失败只说明这条任务在所记录环境中遗漏授权检查，不等于该 Skill 在所有任务中失败。
- 本次没有验证实际博客接入、浏览器效果、其他客户端或版本。
