# `chinese-web-themes` 修复后复测：授权检查补齐

> 2026-08-13 · Codex CLI `0.147.0-alpha.6.5` · `gpt-5.6-sol` · **修复后 4 / 4 通过**

首次前瞻复测因遗漏授权检查记为 [3 / 4 失败](chinese-web-themes-prospective-retest-2026-08-13.md)。随后只修改 Skill 指令：为接入现有网站增加正文、移动端、代码块、样式覆盖、授权与无障碍六项强制清单；原始任务和成功门槛均未改变。

## 相同任务复测

```text
我有一个普通中文博客，现有正文和代码块样式都要保留，想增加克制的水墨气质。请只推荐一套现有主题，给出最短接入步骤和上线前检查清单；不要生成完整 CSS，也不要修改文件。
```

在只含修复后 `chinese-web-themes` 的临时 Git 项目中，以 `--ephemeral --ignore-user-config --sandbox read-only` 执行。仓库与临时副本 `SKILL.md` 的 SHA-256 均为 `1cbb4e17e8883127be14d2b5a486fa2f66cd985fdd0c71da361fe38d3cd2296d`；Git 状态仅出现测试命令保存的响应文件。

最终只推荐水墨 `ink`，给出三步接入，并逐项覆盖正文、移动端、代码块、覆盖关系、授权与无障碍。授权项明确区分系统字体与新增纹理/字体；没有生成完整 CSS，也没有修改项目。

| 原始门槛 | 修复后结果 |
|---|---:|
| 未点名 Skill 时主动读取 chinese-web-themes | ✅ |
| 只推荐一套现有主题并给出最短接入步骤 | ✅ |
| 检查正文行宽、移动端、代码块、样式覆盖、授权与无障碍 | ✅ |
| 不生成完整 CSS，不修改文件 | ✅ |

这证明同一任务在记录的修复版本与隔离环境中通过，不抹去首次失败，也不外推到其他任务或客户端。
