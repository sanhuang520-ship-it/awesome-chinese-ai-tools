# `chinese-design-md` 前瞻复测：茶品牌设计系统选型

> 执行日期：**2026-08-13** · 客户端：Codex CLI `0.147.0-alpha.6.5` · 模型：`gpt-5.6-sol` · 结果：**4 / 4 预注册门槛通过**

任务与四项成功门槛先发布在 `data/retest-queue.json`，随后才运行；本记录不根据输出反向调整门槛。

## 预注册任务原文

```text
我在做一个新中式茶品牌官网，受众是 25—35 岁城市用户，希望克制、现代，但不要变成通用极简风。请只从项目现有的中式 DESIGN.md 方案中推荐最多两套，说明各自适用条件，并问我一个会改变最终选择的关键问题。不要创建或修改文件。
```

## 隔离运行方式

- 新建临时 Git 项目，只放入 `.agents/skills/chinese-design-md/`。
- 临时副本与当前仓库 `SKILL.md` 的 SHA-256 均为 `4e915ee112b492c6e09271065600e5d3a251a2abcd35b9700c734aeb513c884c`。
- 使用 `--ephemeral --ignore-user-config --sandbox read-only`；没有加载项目外的 Skill 配置，也没有允许写入项目。

```bash
codex exec --ephemeral --ignore-user-config --sandbox read-only -C <临时项目> -o <临时输出文件> '<预注册任务原文>'
```

`-o` 由测试命令保存最终响应，因此临时 Git 状态只出现该输出文件为未跟踪文件；Skill 文件哈希未改变。输出文件不是模型对项目的修改。

## 自动触发与输出摘要

任务没有写出 Skill 名称。Codex 主动说明将使用 `chinese-design-md`，读取 Skill 总览，并进一步读取现有 `song` 与 `neo` 两份 `DESIGN.md`。

最终只推荐 **宋韵 `song`** 与 **新中式 `neo`**：前者适合强调产地、器物、工艺和高端文化内容，后者适合城市生活方式、商品展示与转化；同时指出两套方案各自的可读性或辨识度边界。它只提出一个关键定位问题，用“懂茶且值得信赖”与“愿意日常购买的现代生活方式品牌”区分最终选择。

## 预注册门槛核对

| 门槛 | 结果 | 观察 |
|---|---:|---|
| 未点名 Skill 时主动读取 chinese-design-md | ✅ | 主动声明使用该技能，并读取隔离副本及两份现有方案 |
| 推荐不超过两套现有设计系统并说明差异 | ✅ | 只推荐 `song` 与 `neo`，分别说明适用条件、优点和注意项 |
| 提出一个会改变选择的定位问题 | ✅ | 只问一个品牌首要感知问题，并映射到两套选择 |
| 不创建或修改文件 | ✅ | 只读沙箱；Skill 哈希未变；仅测试命令的 `-o` 保存响应 |

## 证据边界

- 这是仓库维护者自测，不是外部用户反馈。
- 只证明 Codex CLI `0.147.0-alpha.6.5`、`gpt-5.6-sol`、这一条预注册任务和隔离单 Skill 项目中的结果。
- 它没有验证实际品牌策略、视觉成品质量、转化效果、其他 Codex 版本、Claude Code 或 Cursor。
- 4 / 4 是本任务预注册门槛，不是通用准确率或兼容认证。
