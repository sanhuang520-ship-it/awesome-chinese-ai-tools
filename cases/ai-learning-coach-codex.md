# Codex 自动触发实测：两周 SQL 学习方案

> 实测日期：**2026-08-12** · Skill：[`ai-learning-coach`](../skills/ai-learning-coach/) · 客户端：Codex CLI `0.147.0-alpha.6.5`

## 原始任务

2026-08-12 使用只读沙箱与临时会话重新复测，逐字任务为：

```text
我想用两周入门 SQL，但不想再看完教程就忘。请先只完成开始学习前的校准：一次问清楚制定计划所必需的信息，不要现在就生成学习计划，也不要修改任何文件。
```

运行方式：

```bash
codex exec --ephemeral --sandbox read-only -C . '<上面的任务>'
```

## 自动触发与结果

Codex 未被点名 Skill，却主动说明将使用 `ai-learning-coach` 做学习前校准，并读取对应 SKILL.md。

它没有生成通用的 14 天课表，而是一次询问：学习用途、当前基础、两周内的时间投入、目标产出、验收标准、学习环境、SQL 数据库方向、学习偏好和过去容易忘的原因，并给出可填写模板。

本次结论是：**自动触发通过，校准任务完成，完整方案等待用户补充信息**。它遵守了“本轮不生成计划、不修改文件”的范围。

## 证据边界

- 本次只验证开始学习前的校准步骤，没有验证后续两周计划、学习循环执行或学习效果。
- 这是 Codex CLI `0.147.0-alpha.6.5`、当前已安装 Skill 集合与单条任务的一次结果，不代表换一种提示或客户端版本仍会触发。
- 运行时 Codex 再次报告部分 Skill description 因 skills 上下文预算被缩短；目标 Skill 本次仍正确触发，但这项告警可能影响其他任务的发现。
