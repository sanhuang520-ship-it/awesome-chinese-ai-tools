# `book-digest-cn` 前瞻复测：无原文时的拆书准备

> 执行日期：**2026-08-13** · 客户端：Codex CLI `0.147.0-alpha.6.5` · 模型：`gpt-5.6-sol` · 结果：**4 / 4 预注册门槛通过**

这不是根据输出倒推出来的案例。任务与四项成功门槛先发布在 `data/retest-queue.json`，随后才运行；旧的《小王子》拆书案例仍单独保留，不被本次结果覆盖。

## 预注册任务原文

```text
我准备给读书会做《小王子》的读书笔记，但现在没有提供目录、摘录或版本信息。请先只完成准备阶段：一次问清楚继续拆解所需的信息，并给我一个“作者问题—核心主张与论证—我的判断”的空白笔记骨架。不要根据印象填写作品内容，不要大段引用原文，也不要修改任何文件。
```

## 隔离运行方式

- 新建临时 Git 项目，只放入 `.agents/skills/book-digest-cn/SKILL.md`。
- 临时副本与当前仓库文件的 SHA-256 均为 `52a5c2457168098986fab0b47a1755b6cb4bd0bc46a89ecd6e24543f6fbcf177`。
- 使用 `--ephemeral --ignore-user-config --sandbox read-only`；没有加载项目外的 Skill 配置，也没有允许写入项目。

```bash
codex exec --ephemeral --ignore-user-config --sandbox read-only -C <临时项目> -o <临时输出文件> '<预注册任务原文>'
```

`-o` 由测试命令保存最终响应，因此临时 Git 状态只出现该输出文件为未跟踪文件；Skill 文件哈希未改变。输出文件不是模型对项目的修改。

## 自动触发与输出摘要

任务没有写出 Skill 名称。Codex 主动说明将使用“中文拆书与读书笔记”技能，并读取临时项目中的 `book-digest-cn/SKILL.md`。

最终响应一次询问了四组信息：版本信息、可供拆解的目录/摘录/批注、阅读与读书会场景、成品形式与篇幅。随后只给出空白骨架，包含“作者问题”“核心主张与论证”“我的判断”三层，并为文本依据、其他解释、作品未回答问题等位置留空。

它没有根据印象填写《小王子》内容，也没有大段引用作品原文。

## 预注册门槛核对

| 门槛 | 结果 | 观察 |
|---|---:|---|
| 未点名 Skill 时主动读取 book-digest-cn | ✅ | 主动声明使用该技能，并读取隔离副本 |
| 询问阅读目的、阅读进度、版本或可核验材料与期望产出 | ✅ | 四组问题覆盖这些信息 |
| 只给空白三层骨架，不填充《小王子》内容 | ✅ | 三层结构均为空白提示或待填写项 |
| 不大段引用原文，不修改文件 | ✅ | 无作品长引文；只读沙箱；Skill 哈希未变 |

## 证据边界

- 这是一个仓库维护者自测，不是外部用户反馈。
- 只证明 Codex CLI `0.147.0-alpha.6.5`、`gpt-5.6-sol`、这一条预注册任务和隔离单 Skill 项目中的结果。
- 它没有验证提供真实书稿后的拆解质量、读书会效果、其他 Codex 版本、Claude Code 或 Cursor。
- 本次没有把“能读取 Skill”外推成跨客户端兼容，也没有把 4 / 4 门槛称为准确率。
