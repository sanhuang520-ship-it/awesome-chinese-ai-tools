# `chinese-lesson-plan` 前瞻复测：45 分钟教案缩小复测

> 执行日期：**2026-08-13** · 客户端：Codex CLI `0.147.0-alpha.6.5` · 模型：`gpt-5.6-sol` · 结果：**4 / 4 预注册门槛通过**

任务与四项成功门槛先发布在 `data/retest-queue.json`，随后才运行；本记录不根据输出反向调整门槛。

## 预注册任务原文

```text
请为小学五年级“水的三态变化”准备一个不超过 300 字的 45 分钟教学时间表，并列出 3 条正式备课前必须核对的教材或安全信息。教材版本没有提供，不要猜单元、页码或课标条目；只做文字输出，不修改文件。
```

## 隔离运行方式

- 新建临时 Git 项目，只放入 `.agents/skills/chinese-lesson-plan/`。
- 临时副本与当前仓库 `SKILL.md` 的 SHA-256 均为 `3ef7d00cb758643a67be66188981017a56aff6ece1ffbe72045acd36a09c8b1e`。
- 使用 `--ephemeral --ignore-user-config --sandbox read-only`；没有加载项目外的 Skill 配置，也没有允许写入项目。

```bash
codex exec --ephemeral --ignore-user-config --sandbox read-only -C <临时项目> -o <临时输出文件> '<预注册任务原文>'
```

`-o` 由测试命令保存最终响应，因此临时 Git 状态只出现该输出文件为未跟踪文件；Skill 文件哈希未改变。

## 自动触发与输出摘要

任务没有写出 Skill 名称。Codex 主动说明按“中国中小学教案”技能处理，读取 `chinese-lesson-plan/SKILL.md` 与科学学科提示。

最终响应共 **282 个字符**。六段时间分别为 5、7、15、10、6、2 分钟，合计 **45 分钟**；内容按观察、预测、分组实验、解释、评价和总结组织。随后恰好列出三条核对项：教材与教师用书、器材与防烫防滑、学校安全制度与学生健康情况。没有填写教材版本、单元、页码或课标条目。

## 预注册门槛核对

| 门槛 | 结果 | 观察 |
|---|---:|---|
| 未点名 Skill 时主动读取 chinese-lesson-plan | ✅ | 主动声明使用教案技能，并读取隔离副本与科学学科提示 |
| 各环节时间合计 45 分钟且全文不超过 300 字 | ✅ | 5 + 7 + 15 + 10 + 6 + 2 = 45；最终响应 282 字符 |
| 列出三条教材或实验安全核对项 | ✅ | 恰好三条，覆盖教材、器材操作与学校/学生安全 |
| 不编教材版本、单元、页码或课标条目 | ✅ | 明确版本中立，未出现这些未经提供的信息 |

## 证据边界

- 这是仓库维护者自测，不是教师或外部用户反馈。
- 只证明记录的 Codex 版本、模型、任务与隔离环境中的结果。
- 它没有验证真实教材匹配、课堂可执行性、教学效果、其他客户端或版本。
- 4 / 4 是本任务预注册门槛，不是通用准确率或教育质量认证。
