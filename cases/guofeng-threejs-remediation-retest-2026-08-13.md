# `guofeng-threejs` 修复后复测：300 字硬门槛

> 2026-08-13 · Codex CLI `0.147.0-alpha.6.5` · `gpt-5.6-sol` · **修复后 4 / 4 通过**

首次前瞻复测虽包含所需内容，但因最终响应 408 字符而记为 [3 / 4 失败](guofeng-threejs-prospective-retest-2026-08-13.md)。随后只修改 Skill 指令：增加短答模板、Unicode 字符计数、220–260 字留余量、相对路径与禁止运行的护栏；原始任务和成功门槛均未改变。

## 相同任务复测

```text
请只审查项目现有水墨 Three.js Demo 的技术思路，不修改、不运行，也不要整文件输出。请在 300 字内列出 3 项水墨 NPR 技法、3 条移动端风险和 2 个相关 Demo 路径，并明确哪些性能或兼容性尚未实测。
```

在只含修复后 `guofeng-threejs` 的临时 Git 项目中，以 `--ephemeral --ignore-user-config --sandbox read-only` 执行。仓库与临时副本 `SKILL.md` 的 SHA-256 均为 `4c4d2f568c84793919755eb6adbdf091d492541ca446d83fe733be07cc87e1c8`；Git 状态仅出现测试命令保存的响应文件。

最终响应按 Python `len(text.strip())` 实测 **294 个 Unicode 字符**。它列出三项技法、三条风险、两个 Demo 路径和未实测项；只做静态读取，没有运行 Demo、修改项目或输出整文件。

| 原始门槛 | 修复后结果 |
|---|---:|
| 未点名 Skill 时主动读取 guofeng-threejs | ✅ |
| 先给结论并将实现核对限制在相关范围 | ✅ |
| 300 字内列出三项技法、三条风险和两个 Demo 路径 | ✅（294 字符） |
| 明确未实测项，不修改、不运行、不整文件输出 | ✅ |

这证明同一任务在记录的修复版本与隔离环境中通过；首次超长失败继续保留，也没有因此声称真机性能已验证。
